[project]
name = "data-pipeline"
version = "0.1.0"
description = ""
authors = [
    {name = "jonathan-JISPlok",email = "jonathanPoli17@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas (>=3.0.5,<4.0.0)"
]

[tool.poetry]
packages = [{include = "data_pipeline", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
